 @using Microsoft.AspNet.Identity

@if (Session["idkorisnik"]!=null)
            {
               
                using (Html.BeginForm("LogOff", "Korisnici", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
                {
                    @Html.AntiForgeryToken()

                    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Pozdrav " + @Session["ime"].ToString() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Registracija", "Create", "Korisnici", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Prijava", "Login", "Korisnici", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
