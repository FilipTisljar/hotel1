@model IEnumerable<Hotel_mvc1.Models.Soba>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.brojSobe)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.brojKreveta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.brojSobe)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.brojKreveta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idsoba }) |
            @Html.ActionLink("Details", "Details", new { id=item.idsoba }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idsoba })
        </td>
    </tr>
}

</table>
